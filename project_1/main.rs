/*
declarations, defintions, etc can be placed outside of fn main()
fn main() is the only spot where other commands can be called

NAMING CONVENTIONS
| Construct | Convention |
|-----------|------------|
| Variables | snake_case |
| Constants | SNAKE_CASE |
| Functions | snake_case |
| Classes   | PascalCase |
| Files     | snake_case |

to compile, use `rustc project_name.rs`
to run, use ./project_name
*/

fn main() {
  println!("Goodnight sky!");
}
